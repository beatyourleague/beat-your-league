# Beat Your League — all public pages are crawlable.
# The sitemap line lands here the day the domain exists; a sitemap requires
# absolute URLs, and inventing a domain would be worse than omitting it.
User-agent: *
Allow: /
